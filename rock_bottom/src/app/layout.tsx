import type { Metadata } from "next";
import { Geist, Geist_Mono, Arsenal, Roboto } from "next/font/google";
import "./globals.css";
import NavBar from "@/features/navs";
import FooterSection from "@/sections/home/footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

const arsenal = Arsenal({
  variable: "--font-arsenal",
  subsets: ["latin"],
  weight: ["400", "700"],
  display: "swap",
});

const roboto = Roboto({
  variable: "--font-roboto",
  subsets: ["latin"],
  weight: ["400", "500", "700"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} ${arsenal.variable} ${roboto.variable} antialiased`}
      >
        <NavBar />
        <div className="pt-[120px]">
          {children}
        </div>
        <FooterSection />
      </body>
    </html>
  );
}
